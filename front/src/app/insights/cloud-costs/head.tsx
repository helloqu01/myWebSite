import { SeoHead } from "@/lib/seo";

export default function Head() {
  return (
    <SeoHead
      title="Cloud Costs: Reduce Waste Without Breaking Systems"
      description="태깅, 리사이징, 캐싱으로 클라우드 비용을 줄이는 실전 가이드."
      path="/insights/cloud-costs"
    />
  );
}
